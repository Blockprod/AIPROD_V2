# District Zero Preproduction Starter Pack

This folder is the new visual preproduction anchor for premium-quality rebuild work.

Use it for:
- locked character identity prompts and hero references
- locked location art direction prompts and set references
- scene-to-location mapping consumed by `--reference-pack`

Current starter file:
- `reference_pack.json`

Current structure:
- `characters/`
- `locations/`

Recommended workflow:
1. add hero stills for main characters and key sets
2. update `reference_pack.json` with those file or URL references
3. regenerate storyboard with `--reference-pack`
4. review character and location stability before any paid video run